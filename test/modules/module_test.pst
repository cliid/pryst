// Test module definition
module TestModule {
    // Test exported function
    export fn add(int a, int b) -> int {
        return a + b;
    }

    // Test exported constant
    export const PI = 3.14159;

    // Test exported string
    export const str MESSAGE = "Hello from TestModule";

    // Test internal function (not exported)
    fn helper() -> void {
        print("Helper function called");
    }
}

// Test module usage
module Main {
    import TestModule;

    export fn main() -> void {
        // Test accessing exported members
        print("PI value: {TestModule::PI}");
        print(TestModule::MESSAGE);

        // Test using exported function
        let result = TestModule::add(10, 20);
        print("Addition result: {result}");

        // Verify that internal members are not accessible
        // This should cause a compilation error:
        // TestModule::helper();  // Uncomment to test error handling
    }
}
