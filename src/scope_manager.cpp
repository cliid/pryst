#include "scope_manager.hpp"
#include "error.hpp"

namespace pryst {

// Implementation is header-only for now since all methods are simple enough
// to be defined inline in the header file. As complexity grows, we can move
// implementations here.

} // namespace pryst
