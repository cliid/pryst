int main() {
    // Test pow function with integer inputs
    float base = 2.0;
    float exponent = 3.0;
    print("Testing pow function:\n");
    print("pow(2.0, 3.0) = ");
    print(pow(base, exponent));
    print("\n");

    // Test pow function with different values
    base = 5.0;
    exponent = 2.0;
    print("pow(5.0, 2.0) = ");
    print(pow(base, exponent));
    print("\n");

    return 0;
}
